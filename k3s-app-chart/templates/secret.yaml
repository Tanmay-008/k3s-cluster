apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
  labels:
    app: {{ .Release.Name }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.appSecrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}